# 路由 basename（子路径部署时设为 /your-app/）
ENV_BASE=/

# 环境标识：test 会加载 eruda；pro 仅白名单+?debug=true
ENV_NAME=pro

# 接口根地址，留空则走当前站点相对路径（可配合 nginx 反代 /api）
VITE_API_BASE_URL=

# docker-compose 宿主机端口
WEB_PORT=8080
